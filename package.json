{
  "name": "pokemon-tcg-marketplace-edge",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that serves the React frontend and routes /api to the ASP.NET Core container.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build:frontend": "cd frontend && REACT_APP_API_URL=/api npm run build",
    "deploy": "npm run build:frontend && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260923.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.136.3"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  }
}
