# Optional. The frontend calls /api on its own origin by default: the Vite dev
# server proxies it to the backend on http://localhost:5259, and in production
# the Cloudflare Worker routes it. Set this only to use a different API.
# VITE_API_URL=http://localhost:5259/api
