# Base URL of the ASP.NET Core backend's API for local development (see
# backend/Properties/launchSettings.json for the port it runs on by default).
# Copy this file to .env and adjust if needed. Production builds use /api,
# because the Cloudflare Worker serves the app and the API from one origin.
REACT_APP_API_URL=http://localhost:5259/api
