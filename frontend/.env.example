# Base URL of the ASP.NET Core backend's API (see backend/Properties/launchSettings.json
# for the port it runs on by default). Copy this file to .env and adjust if needed.
REACT_APP_API_URL=http://localhost:5259/api
